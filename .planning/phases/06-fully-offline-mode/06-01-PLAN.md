---
phase: 06-fully-offline-mode
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/components/OfflineEmptyState.tsx
  - mobile/app/(tabs)/collection.tsx
autonomous: true
requirements:
  - OFFL-01
  - OFFL-02
  - OFFL-03
  - OFFL-04
  - OFFL-05

must_haves:
  truths:
    - "User can see cached albums in collection when offline"
    - "When no cached content exists, user sees empty state with Switch to Standalone button"
  artifacts:
    - path: "mobile/components/OfflineEmptyState.tsx"
      provides: "Reusable empty state component with Switch to Standalone button"
      min_lines: 30
    - path: "mobile/app/(tabs)/collection.tsx"
      provides: "Collection screen with OfflineEmptyState integration"
      contains: "OfflineEmptyState"
  key_links:
    - from: "OfflineEmptyState"
      to: "store.setMode"
      via: "onPress button"
      pattern: "setMode.*standalone"
---

<objective>
Create reusable OfflineEmptyState component with "Switch to Standalone" button and integrate it into Collection screen. This ensures consistent empty state when user is offline with no cached content.
</objective>

<context>
@mobile/components/OfflineBanner.tsx
@mobile/theme/index.ts
@mobile/store/index.ts
@mobile/app/(tabs)/collection.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create OfflineEmptyState component</name>
  <files>mobile/components/OfflineEmptyState.tsx</files>
  <action>
Create `mobile/components/OfflineEmptyState.tsx` with:
- Props: `visible: boolean`, optional `message?: string`
- Default message: "Switch to Standalone mode to download music for offline listening"
- Uses WifiOff icon from lucide-react-native
- Button: "Switch to Standalone" that calls `setMode('standalone')` from store
- Styling: Centered layout, WifiOff icon (size 48), message text in textSecondary color, button with accent background
- Follows theme pattern from mobile/theme/index.ts using useTheme() hook
- Uses useStore for setMode action
- Returns null if not visible
  </action>
  <verify>npx tsc --noEmit mobile/components/OfflineEmptyState.tsx 2>&1 | head -20</verify>
  <done>OfflineEmptyState component exists with WifiOff icon, message, and Switch to Standalone button</done>
</task>

<task type="auto">
  <name>Task 2: Integrate OfflineEmptyState into Collection screen</name>
  <files>mobile/app/(tabs)/collection.tsx</files>
  <action>
In collection.tsx:
1. Import OfflineEmptyState from components
2. Add `isEmpty` computed variable: `const isEmpty = filteredItems.length === 0 && !isCollectionLoading && mode === 'offline'`
3. Add to FlatList's ListEmptyComponent:
   - If isEmpty: show OfflineEmptyState with visible=true
   - Else if isCollectionLoading: show existing loading spinner (ActivityIndicator)
   - Else: null (no empty state for non-offline or loading states)
4. Also add OfflineEmptyState to album_detail screen via separate task (this task is collection only)

Note: The empty state should only appear when in offline mode AND collection is empty AND not loading. Normal "no results" from search should remain unchanged.
  </action>
  <verify>npx tsc --noEmit mobile/app/(tabs)/collection.tsx 2>&1 | head -20</verify>
  <done>Collection screen shows OfflineEmptyState when offline with no cached content</done>
</task>

</tasks>

<verification>
npm run typecheck 2>&1 | grep -E "(error|Error|TS[0-9]+)" | head -10
</verification>

<success_criteria>
1. OfflineEmptyState component renders with WifiOff icon, message, and button
2. Collection screen shows OfflineEmptyState when mode='offline' and filteredItems is empty
3. "Switch to Standalone" button calls setMode('standalone') from store
</success_criteria>

<output>
After completion, create `.planning/phases/06-fully-offline-mode/06-01-SUMMARY.md`
</output>
