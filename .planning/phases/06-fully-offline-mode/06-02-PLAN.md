---
phase: 06-fully-offline-mode
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - mobile/app/(tabs)/artists.tsx
  - mobile/app/album_detail.tsx
autonomous: false
requirements:
  - OFFL-01
  - OFFL-02
  - OFFL-03
  - OFFL-04
  - OFFL-05

must_haves:
  truths:
    - "User can see cached artists in artists screen when offline"
    - "User can navigate album detail and see proper messaging when album not cached"
    - "View on Bandcamp button hidden in offline mode"
  artifacts:
    - path: "mobile/app/(tabs)/artists.tsx"
      provides: "Artists screen with OfflineEmptyState integration"
      contains: "OfflineEmptyState"
    - path: "mobile/app/album_detail.tsx"
      provides: "Album detail with offline empty state handling"
      contains: "offline"
    - path: "mobile/app/artist/artist_detail.tsx"
      provides: "Artist detail with hidden Bandcamp button and OfflineEmptyState"
      contains: "mode !== 'offline'"
  key_links:
    - from: "artist_detail.tsx"
      to: "View on Bandcamp button"
      via: "conditional render"
      pattern: "mode !== 'offline'"
---

<objective>
Integrate OfflineEmptyState into Artists screen and Album Detail screen. Hide "View on Bandcamp" button in offline mode on artist detail. Handle album not found case in offline mode with proper messaging.
</objective>

<context>
@mobile/components/OfflineEmptyState.tsx
@mobile/app/(tabs)/artists.tsx
@mobile/app/album_detail.tsx
@mobile/app/artist/artist_detail.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate OfflineEmptyState into Artists screen</name>
  <files>mobile/app/(tabs)/artists.tsx</files>
  <action>
In artists.tsx:
1. Import OfflineEmptyState from components
2. Compute offline empty state: when mode === 'offline' AND filteredArtists.length === 0 AND no search query (or empty search results that would have matched)
3. Add OfflineEmptyState to the SectionList's ListEmptyComponent
4. The existing ListEmptyComponent shows "No artists found" - wrap this with OfflineEmptyState condition

Note: When searching with searchQuery active, show "No artists found" instead of offline empty state (user might be searching for uncached content while online but not in collection).
  </action>
  <verify>
<automated>npm run test:mobile -- --testPathPattern="store|ScraperService" 2>&1 | tail -30</automated>
</verify>
  <done>Artists screen shows OfflineEmptyState when offline with no artists</done>
</task>

<task type="auto">
  <name>Task 2: Album Detail offline handling</name>
  <files>mobile/app/album_detail.tsx</files>
  <action>
In album_detail.tsx:
1. When in offline mode (mode === 'offline') and getAlbumDetails returns null OR album has 0 tracks, show proper empty state
2. Current behavior: shows "Album not found" which is acceptable, but in offline mode it should be clearer
3. Update the "!album" block (lines 337-350) to check if mode === 'offline':
   - If offline and !album: Show "This album is not available offline" message with "Switch to Standalone" button
   - Use similar styling to existing center view pattern
4. Keep existing behavior for standalone/remote mode (show "Album not found")

Also update the useEffect (lines 99-101) - when in offline mode and album fetch returns null, don't show Alert, just let it fall through to the !album block.
  </action>
  <verify>
<automated>npm run test:mobile -- --testPathPattern="store|ScraperService" 2>&1 | tail -30</automated>
</verify>
  <done>Album detail shows appropriate message when album not cached in offline mode</done>
</task>

<task type="auto">
  <name>Task 3: Hide "View on Bandcamp" button in offline mode</name>
  <files>mobile/app/artist/artist_detail.tsx</files>
  <action>
In artist_detail.tsx:
1. Wrap the "View on Bandcamp" button (lines 349-355) with conditional:
   ```
   {mode !== 'offline' && (
     <TouchableOpacity
       style={[styles.bandcampButton, { backgroundColor: colors.input }]}
       onPress={handleViewOnBandcamp}
     >
       <Text style={[styles.bandcampButtonText, { color: colors.textSecondary }]}>View on Bandcamp</Text>
       <Ionicons name="open-outline" size={16} color={colors.textSecondary} style={{ marginLeft: 5 }} />
     </TouchableOpacity>
   )}
   ```
2. This hides the button in offline mode since it opens external URL (network-dependent)

Also add OfflineEmptyState to artist_detail.tsx ListEmptyComponent (lines 358-366) when mode === 'offline' and filteredArtistItems.length === 0.
  </action>
  <verify>
<automated>npm run test:mobile -- --testPathPattern="store|ScraperService" 2>&1 | tail -30</automated>
</verify>
  <done>"View on Bandcamp" button hidden in offline mode, OfflineEmptyState shown when artist has no cached items</done>
</task>

</tasks>

<verification>
npm run typecheck 2>&1 | grep -E "(error|Error|TS[0-9]+)" | head -10
</verification>

<success_criteria>
1. Artists screen shows OfflineEmptyState when offline with no artists
2. Album detail shows appropriate message when album not cached in offline mode
3. "View on Bandcamp" button hidden in offline mode
4. All TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-fully-offline-mode/06-02-SUMMARY.md`
</output>
